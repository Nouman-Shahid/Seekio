import Navbar from "@/Components/Navbar";
import React from "react";

const FAQ = ({ auth }) => {
    return (
        <>
            <Navbar auth={auth} />
            <div>FAQ</div>
        </>
    );
};

export default FAQ;
